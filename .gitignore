# Dependencies
node_modules/

# Local env (never commit keys) — .env.example stays tracked as the template
.env
!.env.example

# Local agent config (machine/project-specific) — atom.example.json stays tracked
atom.json

# Build / test output
dist/
build/
coverage/
.vite/
*.tsbuildinfo

# Logs & OS junk
*.log
.DS_Store
Thumbs.db

# Local planning / tracker scratch (never published)
plans/
.scratch/
.worktrees/
harness-comparision.md

# Downloaded third-party skills (reproducible via installer; machine-local)
.agents/skills/
skills-lock.json

# Agent-local config and context (machine-specific, never published)
.opencode/
CONTEXT.md
docs/
prompts.md
ROADMAP.md
TUI-DESIGN.md
Session-management.md
AGENTS.md
WebUI-plans.md
.reasonix/skills/stitch-design-taste/SKILL.md

# TUI harness runs + local temps (generated PTY frames, session logs)
runs/
Temp/
.vitest/
.pi/
