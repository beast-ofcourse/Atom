# Dependencies
node_modules/

# Local env (never commit keys) — .env.example stays tracked as the template
.env
!.env.example

# Build / test output
dist/
build/
coverage/
.vite/
*.tsbuildinfo

# Logs & OS junk
*.log
.DS_Store
Thumbs.db

# Local planning / tracker scratch (never published)
plans/
.scratch/
.worktrees/

# Agent-local config and context (machine-specific, never published)
.opencode/
CONTEXT.md
